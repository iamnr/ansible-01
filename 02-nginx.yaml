- name: installing nginx
  hosts: ungrouped
  tasks:
    - name: installign nginx
      ansible.builtin.yum:
        name: nginx
        state: installed
    - name: start nginx
      ansible.builtin.service:
        name: nginx
        state: started